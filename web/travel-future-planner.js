/* Clean next-trip timeline. Renders only when travel/ranking data changes materially. */
(function(){
function esc(s){return typeof tiEsc==='function'?tiEsc(s):String(s??'')}
function money(n){return typeof tiMoney!=='undefined'?tiMoney.format(Number(n)||0):`$${Math.round(Number(n)||0).toLocaleString()}`}
function when(ts){return ts?new Date(Number(ts)*1000).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'}):'—'}
function dur(sec){sec=Math.max(0,Number(sec)||0);const m=Math.round(sec/60);return m<60?`${m}m`:`${Math.floor(m/60)}h ${m%60}m`}
function loadText(t){return (t?.load||[]).map(x=>`${Number(x.buy)||0}× ${esc(x.name||'Item')}`).join(' + ')||'No stocked load'}
function cashNeeded(t){if(Number.isFinite(Number(t?.spend)))return Number(t.spend);return (t?.load||[]).reduce((s,x)=>s+(Number(x.buy)||0)*(Number(x.cost)||0),0)}
function tornAvailableAt(data){const tr=data?.travel||{},now=Date.now()/1000;if(!tr.available)return {known:false,ts:null,exact:false,label:'Travel state unavailable',source:null};if(Number(tr.available_in_torn_at)>0){const ts=Number(tr.available_in_torn_at),exact=!!tr.available_in_torn_exact,source=tr.available_in_torn_source||'backend';return {known:true,ts,exact,source,label:tr.state==='IN_TORN'?'In Torn now':`${exact?'Exact':'Projected'} back in Torn ${when(ts)}`};}if(tr.state==='IN_TORN')return {known:true,ts:now,exact:true,label:'In Torn now',source:'frontend_fallback'};if(tr.state==='FLYING_HOME'&&tr.timestamp)return {known:true,ts:Number(tr.timestamp),exact:true,label:`Back in Torn ${when(tr.timestamp)}`,source:'travel.timestamp_fallback'};return {known:false,ts:null,exact:false,label:'Waiting for return time',source:null};}
function pickRestock(trip,home){if(!home.known)return null;const flight=(Number(trip.one_way_minutes)||0)*60,current=Number(trip.load?.[0]?.profit_each)||0;let best=null;for(const r of (trip.restocks||[])){if(!r.estimated_at||r.status==='LEARNING')continue;const conf={HIGH:3,MEDIUM:2,LOW:1}[String(r.confidence_label||'').toUpperCase()]||0;if(conf<2)continue;const target=Number(r.estimated_at)-flight;if(target<home.ts-120)continue;const profit=Number(r.profit_each)||0;if(profit<=current*1.15)continue;const c={r,target,profit,conf};if(!best||c.profit>best.profit*1.08||(c.profit>=best.profit*.92&&c.conf>best.conf))best=c;}return best;}
function timing(trip,home){if(!home.known)return {depart:null,arrive:null,returnAt:null,restock:null};const flight=(Number(trip.one_way_minutes)||0)*60,restock=pickRestock(trip,home);const depart=restock?Math.max(home.ts,restock.target):home.ts;return {depart,arrive:depart+flight,returnAt:depart+flight*2,restock};}
function confidenceBadge(label,type){const v=String(label||'LOW').toUpperCase(),cls=v==='HIGH'?'good':v==='MEDIUM'?'warn':'bad';return `<span class="nt-conf ${cls}">${esc(type)} ${esc(v)}</span>`}
function timeline(trip,home){const t=timing(trip,home),state=(window.tiLastData?.travel?.state)||'IN_TORN';const now=Date.now()/1000;const nodes=[];
 if(state!=='IN_TORN')nodes.push({label:'NOW',time:'Away',detail:state.replaceAll('_',' '),kind:'muted'});
 nodes.push({label:'BACK IN TORN',time:home.known?when(home.ts):'TBD',detail:home.exact?'exact':'projected',kind:home.exact?'done':'muted'});
 nodes.push({label:'DEPART',time:t.depart?when(t.depart):'TBD',detail:t.depart&&t.depart>Math.max(now,home.ts)+60?`wait ${dur(t.depart-home.ts)}`:'as soon as ready',kind:'go'});
 nodes.push({label:`ARRIVE ${String(trip.country_name||'').toUpperCase()}`,time:t.arrive?when(t.arrive):'TBD',detail:`${Math.round(Number(trip.one_way_minutes)||0)}m flight`,kind:'go'});
 nodes.push({label:'BACK IN TORN',time:t.returnAt?when(t.returnAt):'TBD',detail:'after return flight',kind:'muted'});
 return `<div class="nt-line">${nodes.map((n,i)=>`<div class="nt-node ${n.kind}"><div class="nt-dot"></div><span>${esc(n.label)}</span><strong>${esc(n.time)}</strong><small>${esc(n.detail)}</small></div>${i<nodes.length-1?'<div class="nt-rail"></div>':''}`).join('')}</div>`;
}
function restockBox(trip,home){const t=timing(trip,home),r=t.restock?.r;if(!r)return `<div class="nt-restock quiet"><span>RESTOCK WATCH</span><strong>No higher-value timed restock beats the current load.</strong><small>Current-stock recommendation is being used.</small></div>`;const delta=Number(r.estimated_at)-Number(t.arrive||0);return `<div class="nt-restock"><div><span>RESTOCK WATCH</span><strong>${esc(r.name)}</strong><small>Estimated ${when(r.estimated_at)} · ${delta<0?`${dur(Math.abs(delta))} before arrival`:`${dur(delta)} after arrival`} · ${Number(r.sample_cycles)||0} observed cycle${Number(r.sample_cycles)===1?'':'s'}</small></div>${confidenceBadge(r.confidence_label,'RESTOCK')}</div>`}
function primary(trip,home){const t=timing(trip,home),stockConf=trip.confidence_label||'LOW';return `<article class="nt-primary"><div class="nt-head"><div><span>RECOMMENDED NEXT TRIP</span><h3>${esc(trip.country_name||trip.country)}</h3><small>${loadText(trip)}</small></div><div class="nt-go">GO</div></div>${timeline(trip,home)}<div class="nt-stats"><div><span>CASH NEEDED</span><strong>${money(cashNeeded(trip))}</strong></div><div><span>EXPECTED PROFIT</span><strong>${money(trip.expected_profit)}</strong></div><div><span>PROFIT / HOUR</span><strong>${money(trip.profit_per_hour)}</strong></div><div><span>ROUND TRIP</span><strong>${dur((Number(trip.round_trip_minutes)||0)*60)}</strong></div></div><div class="nt-confidence"><div>${confidenceBadge(stockConf,'STOCK / PRICE')}<small>Freshness/reliability of the current stock + resale snapshot.</small></div></div>${restockBox(trip,home)}</article>`}
function alt(trip,rank,home){const t=timing(trip,home),r=t.restock?.r;return `<article class="nt-alt"><div><span>OPTION ${rank}</span><strong>${esc(trip.country_name||trip.country)}</strong><small>${loadText(trip)}</small></div><div class="nt-alt-meta"><span>Leave ${t.depart?when(t.depart):'TBD'}</span><span>${money(cashNeeded(trip))} cash</span><span>${money(trip.expected_profit)} profit</span><span>${esc(trip.confidence_label||'LOW')} stock/price</span>${r?`<span>${esc(r.name)} restock ${when(r.estimated_at)} · ${esc(r.confidence_label)} conf.</span>`:''}</div></article>`}
let lastKey='';
function keyFor(data){const tr=data?.travel||{},trips=(data?.trips||[]).slice(0,3);return JSON.stringify({state:tr.state,at:tr.available_in_torn_at,exact:tr.available_in_torn_exact,dest:tr.destination_code,trips:trips.map(t=>({c:t.country,p:t.expected_profit,s:t.spend,conf:t.confidence_label,load:(t.load||[]).map(x=>[x.item_id,x.buy,x.stock]),r:(t.restocks||[]).slice(0,2).map(x=>[x.item_id,x.estimated_at,x.confidence_label,x.sample_cycles])}))})}
function render(force=false){const root=document.getElementById('futurePlanner');const data=window.tiLastData||((typeof tiLastData!=='undefined')?tiLastData:null);if(!root||!data)return;const key=keyFor(data);if(!force&&key===lastKey)return;lastKey=key;const trips=(data.trips||[]).slice(0,3),home=tornAvailableAt(data);if(!trips.length){root.innerHTML='<div class="empty">No profitable future trip is currently available.</div>';return;}const tr=data.travel||{};const status=tr.state==='IN_TORN'?'Ready now':home.label;root.innerHTML=`<div class="nt-status"><div><span>NEXT-TRIP TIMELINE</span><strong>${esc(status)}</strong><small>${home.exact?'Uses exact Torn travel timing where available.':'Projected until Torn provides an exact return-flight timestamp.'}</small></div><div class="nt-live"><i></i> LIVE PLAN</div></div>${primary(trips[0],home)}${trips.length>1?`<details class="nt-options"><summary>Other good trips</summary><div class="nt-alt-grid">${trips.slice(1).map((t,i)=>alt(t,i+2,home)).join('')}</div></details>`:''}`;}
window.tiRenderFuturePlanner=()=>render(true);
const css=document.createElement('style');css.textContent=`.nt-status{display:flex;justify-content:space-between;gap:12px;align-items:center;margin:8px 0 12px;padding:10px 12px;border:1px solid var(--border,#343943);border-radius:9px;background:rgba(255,255,255,.02)}.nt-status span,.nt-head span,.nt-stats span,.nt-restock span,.nt-alt>div>span{font-size:9px;font-weight:900;letter-spacing:.1em;color:var(--muted,#9ba1ad)}.nt-status strong,.nt-status small{display:block}.nt-status strong{font-size:14px;margin-top:2px}.nt-status small{font-size:9px;color:var(--muted,#9ba1ad);margin-top:2px}.nt-live{font-size:9px;font-weight:900;color:#70d89a;white-space:nowrap}.nt-live i{display:inline-block;width:7px;height:7px;border-radius:50%;background:#55c986;margin-right:5px;box-shadow:0 0 0 3px rgba(85,201,134,.12)}.nt-primary{border:1px solid rgba(85,201,134,.45);border-radius:12px;padding:14px;background:linear-gradient(180deg,rgba(85,201,134,.065),rgba(255,255,255,.015))}.nt-head{display:flex;justify-content:space-between;gap:12px}.nt-head h3{font-size:23px;margin:3px 0}.nt-head small{color:var(--muted,#9ba1ad)}.nt-go{align-self:flex-start;padding:7px 10px;border-radius:8px;background:rgba(85,201,134,.13);color:#70d89a;font-weight:900}.nt-line{display:flex;align-items:flex-start;margin:18px 0 15px;overflow-x:auto;padding:0 2px 5px}.nt-node{min-width:105px;text-align:center;position:relative}.nt-dot{width:11px;height:11px;border-radius:50%;margin:0 auto 7px;background:#657080;border:2px solid #252a31;box-shadow:0 0 0 2px #657080}.nt-node.go .nt-dot{background:#55c986;box-shadow:0 0 0 2px #55c986}.nt-node.done .nt-dot{background:#75b7ff;box-shadow:0 0 0 2px #75b7ff}.nt-node span,.nt-node strong,.nt-node small{display:block}.nt-node span{font-size:8px;font-weight:900;letter-spacing:.06em;color:var(--muted,#9ba1ad);white-space:nowrap}.nt-node strong{font-size:13px;margin-top:2px}.nt-node small{font-size:8px;color:var(--muted,#9ba1ad);margin-top:2px}.nt-rail{height:2px;background:#3a424d;flex:1;min-width:35px;margin-top:5px}.nt-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:4px}.nt-stats>div{padding:9px;border:1px solid var(--border,#343943);border-radius:8px;background:rgba(0,0,0,.12)}.nt-stats strong{display:block;font-size:14px;margin-top:3px}.nt-confidence{display:flex;gap:8px;margin-top:10px}.nt-confidence>div{display:flex;align-items:center;gap:8px}.nt-confidence small{font-size:9px;color:var(--muted,#9ba1ad)}.nt-conf{display:inline-block;padding:4px 7px;border-radius:999px;font-size:8px;font-weight:900;letter-spacing:.05em;white-space:nowrap}.nt-conf.good{background:rgba(85,201,134,.12);color:#70d89a}.nt-conf.warn{background:rgba(244,199,109,.12);color:#f4c76d}.nt-conf.bad{background:rgba(224,139,101,.12);color:#e08b65}.nt-restock{display:flex;justify-content:space-between;gap:10px;align-items:center;margin-top:10px;padding:9px 10px;border:1px solid rgba(216,168,79,.25);border-radius:8px;background:rgba(216,168,79,.04)}.nt-restock strong,.nt-restock small{display:block}.nt-restock strong{font-size:13px;margin-top:2px}.nt-restock small{font-size:9px;color:var(--muted,#9ba1ad);margin-top:2px}.nt-restock.quiet{border-color:var(--border,#343943);background:rgba(255,255,255,.015)}.nt-options{margin-top:10px}.nt-options summary{cursor:pointer;color:var(--muted,#9ba1ad);font-size:10px;font-weight:800}.nt-alt-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px}.nt-alt{padding:10px 11px;border:1px solid var(--border,#343943);border-radius:9px;background:rgba(255,255,255,.018)}.nt-alt strong,.nt-alt small{display:block}.nt-alt strong{font-size:15px;margin-top:2px}.nt-alt small{font-size:9px;color:var(--muted,#9ba1ad);margin-top:2px}.nt-alt-meta{display:flex;flex-wrap:wrap;gap:5px;margin-top:7px}.nt-alt-meta span{font-size:9px;padding:3px 6px;border-radius:5px;background:rgba(255,255,255,.035);color:var(--muted,#9ba1ad)}@media(max-width:800px){.nt-stats{grid-template-columns:1fr 1fr}.nt-alt-grid{grid-template-columns:1fr}.nt-confidence>div{align-items:flex-start;flex-direction:column}.nt-node{min-width:92px}}`;document.head.appendChild(css);
setTimeout(()=>render(true),250);setInterval(()=>render(false),2500);
})();
